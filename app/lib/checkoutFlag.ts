/**
 * Тимчасовий запобіжник на час перебудови лендингу під ширше позиціонування
 * платформи (product vision doc). Лендинг зараз описує модулі (Sites, AI,
 * Content, Rank, Analytics), яких фізично ще не існує в коді — щоб ніхто
 * не міг реально оплатити неіснуючий функціонал через LemonSqueezy, checkout
 * вимкнено цим прапорцем.
 *
 * Коли модулі з product vision будуть побудовані і відповідатимуть тому,
 * що написано на лендингу — просто зміни на false (або видали прапорець
 * і прибери всі перевірки CHECKOUT_DISABLED), і checkout-кнопки знову
 * запрацюють як раніше.
 */
export const CHECKOUT_DISABLED = true;
